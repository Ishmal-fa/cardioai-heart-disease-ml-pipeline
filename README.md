# cardioai-heart-disease-ml-pipeline
End-to-end machine learning pipeline for heart disease prediction and handwritten digit recognition. Implements preprocessing, clustering (K-Means, hierarchical), ensemble models (Random Forest, XGBoost), neural networks (SLP, MLP, CNN), and a Streamlit dashboard for interpretable clinical predictions.
